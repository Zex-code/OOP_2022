import turtle
import time
turtle.shape('turtle')

for i in range(12):
    for i in range(1):
        turtle.forward(50)
        turtle.stamp()
        turtle.backward(50)
        turtle.left(30)
time.sleep(5)
