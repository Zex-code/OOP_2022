import turtle
import time
turtle.shape('turtle')
turtle.tracer(2)
def foo ():
    for i in range(360):
        turtle.forward(1)
        turtle.left(1)
    for i in range(360):
        turtle.forward(1)
        turtle.right(1)
for i in range(3):
    foo()
    turtle.left(60)
time.sleep(5)
